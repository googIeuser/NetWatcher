name: Legacy Go CI (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Go CI is disabled. Use Rust Flutter Preview instead."
