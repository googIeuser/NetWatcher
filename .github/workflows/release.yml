name: Legacy Go Release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Go release is disabled. Build stable artifacts with NetWatcher Stable Build and publish the GitHub Release manually."
