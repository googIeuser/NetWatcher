name: NetWatcher Preview Build (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    name: Preview builds are disabled
    runs-on: ubuntu-latest
    steps:
      - name: Stable release information
        run: |
          echo "Automatic portable preview builds are disabled."
          echo "Use Actions > NetWatcher Stable Release > Run workflow."
