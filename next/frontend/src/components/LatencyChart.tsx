import type {TargetStatus} from '../types'
export function LatencyChart({targets,title,eyebrow,rangeLabel}:{targets:TargetStatus[];title:string;eyebrow:string;rangeLabel:string}){const preferred=targets.find(x=>x.target.name==='Cloudflare'&&x.history.length)||targets.find(x=>x.history.length);const samples=preferred?.history??[];const values=samples.map(x=>x.success?x.latency:0);const max=Math.max(40,...values);const points=values.map((v,i)=>`${(i/Math.max(1,values.length-1))*100},${82-(v/max)*66}`).join(' ');return <section className="panel chart-panel"><div className="panel-heading"><div><span className="eyebrow">{eyebrow}</span><h2>{title}</h2></div><div className="range-pill">{rangeLabel}</div></div><div className="chart-wrap"><div className="chart-grid"><span>0 ms</span><span>{Math.round(max/2)} ms</span><span>{Math.round(max)} ms</span></div><svg className="chart" viewBox="0 0 100 90" preserveAspectRatio="none"><defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="currentColor" stopOpacity=".28"/><stop offset="100%" stopColor="currentColor" stopOpacity="0"/></linearGradient></defs>{points&&<><polygon points={`0,86 ${points} 100,86`} fill="url(#area)"/><polyline points={points} fill="none" stroke="currentColor" strokeWidth="1.7" vectorEffect="non-scaling-stroke"/></>}</svg>{!points&&<div className="empty-chart">—</div>}</div></section>}
