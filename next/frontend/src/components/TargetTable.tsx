import type {TargetStatus} from '../types'
import type {TranslationKey} from '../lib/i18n'
const fmt=(v:number,s=' ms')=>v>0?`${v.toFixed(1)}${s}`:'—'
export function TargetTable({targets,t}:{targets:TargetStatus[];t:(k:TranslationKey)=>string}){return <section className="panel target-panel"><div className="panel-heading"><div><span className="eyebrow">{t('endpoints').toUpperCase()}</span><h2>{t('monitoredTargets')}</h2></div><span className="target-count">{targets.length}</span></div><div className="target-list">{targets.map(item=><article className="target-row" key={item.target.id}><div className={`status-dot ${item.state}`}/><div className="target-identity"><strong>{item.target.name}</strong><span title={item.target.host}>{item.target.host} · {item.target.mode.toUpperCase()}</span></div><div className="target-stat"><span>{t('latency')}</span><strong>{fmt(item.latency)}</strong></div><div className="target-stat"><span>{t('packetLoss')}</span><strong>{fmt(item.packetLoss,'%')}</strong></div><div className="target-stat"><span>{t('jitter')}</span><strong>{fmt(item.jitter)}</strong></div><span className={`state-label ${item.state}`}>{t(item.state as TranslationKey)}</span></article>)}</div></section>}
