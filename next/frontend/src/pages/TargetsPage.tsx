import {useState} from 'react'
import type {Settings,Snapshot} from '../types'
import type {TranslationKey} from '../lib/i18n'
export function TargetsPage({snapshot,settings,onAdd,onEdit,onRemove,t}:{snapshot:Snapshot;settings:Settings;onAdd(v:string):Promise<void>;onEdit(a:string,b:string):Promise<void>;onRemove(v:string):Promise<void>;t:(k:TranslationKey)=>string}){const [value,setValue]=useState('');const [editing,setEditing]=useState('');const [editValue,setEditValue]=useState('');const [busy,setBusy]=useState(false);const add=async()=>{if(!value.trim())return;setBusy(true);try{await onAdd(value.trim());setValue('')}finally{setBusy(false)}};const saveEdit=async()=>{if(!editing||!editValue.trim())return;setBusy(true);try{await onEdit(editing,editValue.trim());setEditing('')}finally{setBusy(false)}};return <div className="page"><header className="page-header compact"><div><span className="eyebrow">{t('targetManager').toUpperCase()}</span><h1>{t('targets')}</h1><p>{t('targetHelp')}</p></div></header><section className="panel target-editor"><div className="input-group"><label htmlFor="target">{t('newTarget')}</label><div><input id="target" value={value} onChange={e=>setValue(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void add()} placeholder="example.com or tcp://example.com:443"/><button disabled={busy||!value.trim()} onClick={()=>void add()}>{t('addTarget')}</button></div></div></section><section className="panel"><div className="panel-heading"><div><span className="eyebrow">{t('customTargets').toUpperCase()}</span><h2>{t('savedEndpoints')}</h2></div></div><div className="saved-targets">{settings.custom_targets.length===0&&<div className="empty-list">{t('noTargets')}</div>}{settings.custom_targets.map(target=>{const live=snapshot.targets.find(x=>x.target.custom&&(x.target.host===target||target.includes(x.target.host)));const isEdit=editing===target;return <article key={target}>{isEdit?<div className="inline-edit"><input value={editValue} onChange={e=>setEditValue(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void saveEdit()}/><button className="text-button" onClick={()=>void saveEdit()}>{t('save')}</button><button className="text-button" onClick={()=>setEditing('')}>{t('cancel')}</button></div>:<><div><strong>{target}</strong><span>{live?.message||t('waiting')}</span></div><span className={`state-label ${live?.state||'waiting'}`}>{t((live?.state||'waiting') as TranslationKey)}</span><div className="row-actions"><button className="text-button" onClick={()=>{setEditing(target);setEditValue(target)}}>{t('edit')}</button><button className="text-button danger" onClick={()=>window.confirm(t('confirmRemove'))&&void onRemove(target)}>{t('remove')}</button></div></>}</article>})}</div></section></div>}
